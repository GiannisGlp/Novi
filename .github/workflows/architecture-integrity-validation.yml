name: Architecture Integrity Validation

on:
  pull_request:
  push:
    branches:
      - main
      - develop

jobs:
  architecture-integrity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate architecture references
        run: python3 scripts/validate_architecture_integrity.py
